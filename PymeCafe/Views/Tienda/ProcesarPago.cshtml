<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">
    <title>Confirmación de Pago</title>
    <style>
        body {
            background-color: #f8f9fa;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .confirmation-box {
            background: #ffffff;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 500px;
            width: 100%;
        }

            .confirmation-box h1 {
                color: #28a745;
                font-size: 2.5rem;
                margin-bottom: 1rem;
            }

            .confirmation-box p {
                color: #6c757d;
                font-size: 1rem;
                margin-bottom: 1.5rem;
            }

            .confirmation-box .btn-primary {
                background-color: #007bff;
                border-color: #007bff;
                padding: 0.5rem 2rem;
                font-size: 1rem;
                border-radius: 5px;
                transition: background-color 0.3s ease;
            }

                .confirmation-box .btn-primary:hover {
                    background-color: #0056b3;
                }
    </style>
</head>
<body>
    <div class="confirmation-box animate__animated animate__fadeIn">
        <h1 class="animate__animated animate__bounceInDown">¡Gracias por tu compra!</h1>
        <p class="animate__animated animate__fadeInUp">Tu pedido ha sido procesado exitosamente. Recibirás un correo con los detalles.</p>
        <a href="@Url.Action("Index", "Tienda")" class="btn btn-primary animate__animated animate__zoomIn">Volver a la Tienda</a>
    </div>
</body>
</html>
