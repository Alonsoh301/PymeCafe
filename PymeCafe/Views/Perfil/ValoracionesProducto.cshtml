@model IEnumerable<PymeCafe.Models.Valoracionesdeproducto>

<h2>Valoraciones de Producto</h2>

@if (!Model.Any())
{
    <p>No has realizado valoraciones de productos.</p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Producto</th>
                <th>Calificación</th>
                <th>Comentario</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var valoracion in Model)
            {
                <tr>
                    <td>@valoracion.Producto</td> <!-- Cambia "Producto" si el nombre del campo es diferente -->
                    <td>@valoracion.Calificacion</td> <!-- Cambia "Calificacion" si el nombre del campo es diferente -->
                    <td>@valoracion.Comentario</td> <!-- Cambia "Comentario" si el nombre del campo es diferente -->
                </tr>
            }
        </tbody>
    </table>
}
